# Versão 0.1.0a3

## Desenvolvimento

Comando `command2` virou `strings` para testar `oxigenpy.strings`.

Indicada dependencia de `oxigenpy==0.1.0a1` no `pyproject.toml`.

**$** Como essa biblioteca está no `test.pypi` a instalação do projeto deve ser assim:
```bash
uv pip install -e . \
  --index-url https://test.pypi.org/simple/ \
  --extra-index-url https://pypi.org/simple
```

**$** teste
```bash
libtest timeid
```

**>**
```bash
20260120-222304-057133-pid189533-tid140167649384256
```
